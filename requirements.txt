# ──────────────────────────────────────────────────────────────────────────────
#  Dépendances runtime — versions épinglées pour des builds reproductibles
# ──────────────────────────────────────────────────────────────────────────────

# Framework bot Telegram asynchrone (3.x)
aiogram==3.13.1

# Configuration typée via variables d'environnement
pydantic==2.9.2
pydantic-settings==2.5.2

# ORM + migrations
SQLAlchemy[asyncio]==2.0.35
alembic==1.13.3
aiosqlite==0.20.0          # driver async SQLite
asyncpg==0.29.0            # driver async PostgreSQL (Railway / prod)

# Téléchargement de médias
yt-dlp==2024.11.4          # backend vidéos
gallery-dl==1.27.6         # backend "tous médias" (photos + vidéos)

# Scraping de secours (listing par compte)
twscrape==0.13.0

# Logging structuré JSON
structlog==24.4.0

# HTTP / réseau bas niveau (déjà tiré par aiogram, épinglé pour cohérence)
aiohttp==3.10.10

# Utilitaires
tenacity==9.0.0            # primitives de retry (complément au retry maison)
python-dotenv==1.0.1       # chargement .env en local
